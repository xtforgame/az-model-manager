import sequelize, {
  Sequelize,
  HasManyAddAssociationMixin,
  HasManyCreateAssociationMixin,
} from 'sequelize';
import { Overwrite, ExtendedModel } from 'library';

{% for name in orders -%}
  {%- if schemas.models[name] -%}
    {% render 'model.liquid', name: name, model: schemas.models[name] %}
  {%- endif -%}
  {%- if schemas.associationModels[name] -%}
    {% render 'model.liquid', name: name, model: schemas.associationModels[name] %}
  {%- endif -%}
{%- endfor -%}
